# Miki AI 工作台 · 产品需求文档（PRD）

> **本地优先的个人运营中枢**
> 版本 v0.1（草案） · 创建 2026-08-13 · 作者 Kai × Miki · 状态 待评审

---

## 目录

1. [产品概述](#1-产品概述)
2. [用户与场景](#2-用户与场景)
3. [功能需求](#3-功能需求)
4. [策略设计（AI）](#4-策略设计ai-部分)
5. [非功能需求](#5-非功能需求)
6. [技术架构](#6-技术架构)
7. [数据隐私与安全](#7-数据隐私与安全)
8. [发布计划 / 路线图](#8-发布计划--路线图)
9. [风险与开放问题](#9-风险与开放问题)
10. [成功指标](#10-成功指标)
11. [附录](#11-附录)

> **文档定位**：本 PRD 将当前已交付的「本地可编辑工作台」定义为产品的 **MVP**，并据此补全产品愿景、功能边界与迭代路线图。方法论参考 2026 年个人 AI 工作台主流范式（local-first 本地优先、记忆复利、四层架构）与标准 PRD 章节。

---

## 1. 产品概述

### 1.1 产品定位与一句话价值

**本地优先、AI 协作驱动的「个人运营中枢」**：把散落在对话、自动化任务、记忆、技能与每日亮点中的个人资产，统一收敛为可检索、可编辑、可沉淀的结构化工作台。

### 1.2 背景与痛点

Miki 处于自由职业起步期，三大方向为自媒体（纯图文 / AI 生成，不露脸不录音）、电商（轻资产）、AI 技术学习。当前个人资产高度碎片化：

- **信息散落**：定时任务在 `automations` 数据库、记忆在 `MEMORY.md`、技能在 `skills/` 目录、亮点在 `highlights.md`，彼此割裂。
- **缺乏沉淀**：对话产物用完即弃，难以回溯与复用。
- **整理耗时**：手动在多个文件 / 平台间搬运，打断心流。
- **飞书实时双向同步结构性不可行**：lark-cli 事件订阅不支持多维表格域、飞书记录无单条修改时间 → 无法做「实时回写 / 时间戳新者胜」→ 转向本地优先架构。

### 1.3 产品愿景

成为 Miki 的「**数字联合创始人**」：不止记录，更能主动组织、提醒、复用个人知识资产，随时间复利增值。对应 2026 年 AI workspace 范式的核心命题——*把注意力从「工具搬运」转向「意图执行」*。

### 1.4 核心价值（USP）

| 差异化 | 说明 |
| --- | --- |
| 本地优先、隐私可控 | 仅绑定 127.0.0.1，不上云、不做任何云端同步；数据始终在本人机器上。 |
| 与运行时同源 | `automations` / `MEMORY` / `skills` / `highlights` 即真实数据源，编辑即写回，不存在「影子副本」。 |
| 开源、人人可装 | 定位 local-first 开源工具：`git clone` + 一条命令本地启动，自行托管、无需账号，暂不考虑商业化。 |
| 误删可恢复 | 每次写操作前自动生成 `.bak` 备份。 |
| 零依赖轻量 | Python 标准库 + 原生前端，双击 `.bat` 即可启动，启动资金几乎为 0。 |

### 1.5 目标平台

- **MVP**：Windows 桌面（本地 HTTP 服务 + 浏览器），双击 bat 启动、用完关闭。
- **规划**：macOS / Linux 适配；移动端只读视图（v2.0）。

### 1.6 范围（In / Out of Scope）

| In Scope（当前） | Out of Scope（当前） |
| --- | --- |
| 4 类资产的查看、搜索、筛选、编辑、删除、新增；本地写回与 `.bak` 备份 | 云端多端实时同步、多人协作、AI 自动生成内容、第三方平台（飞书 / 微信 / 电商后台）对接 |

---

## 2. 用户与场景

### 2.1 用户画像（Persona）

| 字段 | 内容 |
| --- | --- |
| 姓名 / 身份 | Miki · 自由职业者（单人公司 / solopreneur） |
| 方向 | 自媒体（幕后纯图文 / AI 生成）、电商（轻资产）、AI 技术学习 |
| 技能背景 | 技术 / 开发类，能读懂脚本，偏好务实干练 |
| 约束 | 启动资金 < 5000；前 6 个月月入目标 5000–1 万；全部时间可投入 |
| 偏好 | 不露脸不录音、轻资产、本地优先、出镜意愿为零 |

### 2.2 核心用户故事

1. 作为自由职业者，我想在一个地方看到今天 / 累计的定时任务、记忆、技能和亮点，**以便掌控全局**。
2. 作为内容创作者，我想快速记录一条「决策 / 洞察」亮点，**以便日后复盘**。
3. 作为技能使用者，我想修改某个 Skill 的中文说明，**以便自己和后续对话快速理解其用途**。
4. 作为记忆管理者，我想删除一条过时的记忆小节，**以便保持知识库干净**。
5. 作为单人运营，我希望所有改动都自动备份，**以便误删可恢复**。

### 2.3 用户旅程（主路径）

双击 `start-workbench.bat` → 浏览器自动打开工作台 → 切换 Tab / 搜索 / 按字段筛选 → 点「编辑」改字段或「删除」移除 → 用完点「关闭工作台」。

---

## 3. 功能需求

### 3.1 功能架构（模块地图）

| 模块 | 职责 | 优先级 | 状态 |
| --- | --- | --- | --- |
| M1 工作台视图（前端） | 4 Tab 表格、搜索、筛选、行内编辑 / 删除操作列 | P0 | 已交付 |
| M2 定时任务管理 | 查看 / 编辑（名称、状态）/ 软删除 | P0 | 已交付 |
| M3 记忆管理 | 查看 / 编辑内容 / 删除 ## 小节 | P0 | 已交付 |
| M4 技能管理 | 查看 / 编辑中文功能说明（禁用删除） | P0 | 已交付 |
| M5 每日亮点 | 新增 / 编辑 / 删除亮点条目 | P0 | 已交付 |
| M6 数据写回与备份 | 写前生成 `.bak`，字段级最小改写 | P0 | 已交付 |
| M7 安全与权限 | 仅本机绑定、关闭端点、无外部网络 | P0 | 已交付 |
| M8 定时任务新建 | 在界面内创建自动化任务（写入 automations 数据库，与运行时同源） | P1 | 已交付 |
| M9 AI 自动亮点捕获 | 对话中主动识别并写入亮点 | P1 | 规划 |
| M10 跨平台 / 移动只读 | macOS / Linux、手机查看 | P2 | 规划 |

### 3.2 模块详述（用户故事 + 逻辑 + 验收）

#### M1 工作台视图（P0 · 已交付）
- **功能**：4 个 Tab（定时任务 / 记忆 / Skill / 每日亮点），顶部全文搜索，按字段下拉筛选，飞书风格表格，粘性表头（随头部高度自适应），每行统一的「编辑 / 删除」操作列。
- **验收**：① 4 分区数据正常加载并带数量徽标；② 搜索 / 筛选实时生效；③ 每个分区行尾均可见操作列。

#### M2 定时任务管理（P0 · 已交付）
- **字段**：名称、状态（运行中 / 已暂停）、频率、说明、下次运行。
- **编辑**：名称（文本）、状态（下拉切换）。
- **删除**：软删除——置 `deleted_at`，视图隐藏但数据库可恢复。
- **验收**：改状态后数据库即时生效；删除后不再出现在视图；所有动作生成 `.bak`。

#### M3 记忆管理（P0 · 已交付）
- **数据源**：全局 `~/.workbuddy/MEMORY.md` + 项目 `.workbuddy/memory/MEMORY.md` 的 `##` 小节。
- **编辑**：内容（多行文本，写回对应小节）。**删除**：移除整个 `##` 小节。
- **验收**：改写精确落盘对应 MEMORY.md；删除后小节消失；备份文件存在。

#### M4 技能管理（P0 · 已交付）
- **数据源**：用户级 + 项目级 `skills/*/SKILL.md`。
- **编辑**：功能说明（写入 `skill_cn_override.json` 覆盖）。
- **删除**：**禁止**（删除技能目录风险高，误删难恢复）。UI 不显示删除按钮。
- **验收**：说明改动反映在 override 并重新加载生效；删除入口不存在。

#### M5 每日亮点（P0 · 已交付）
- **格式**：`- [id=HL-...] [类型] 内容`；类型：决策 / 学习 / 洞察 / 任务 / 其他。
- **新增 / 编辑 / 删除**：均写回 `highlights.md`，删除移除对应行。
- **验收**：新增追加到文件末尾；改类型 / 内容写回；删除该行消失。

#### M6 数据写回与备份（P0 · 已交付）
- **逻辑**：每次写前 `shutil.copy2` 生成 `.bak`；采用字段级最小改写（正则替换单行 / 小节），而非整文件重写。
- **验收**：任一写操作后，目标文件同目录存在对应 `.bak`。

### 3.3 优先级（MoSCoW）

| 层级 | 内容 |
| --- | --- |
| Must（必须有） | M1–M7 当前能力（4 分区增改删查 + 备份） |
| Should（应该有） | M8 定时任务新建、亮点按日期排序 / 筛选增强、空状态引导、记忆合并去重 |
| Could（可以有） | M9 AI 自动亮点捕获、M10 跨平台 / 移动只读、明暗主题切换、数据导出 |
| Won't（本期不做） | 云端实时同步、多人协作、第三方平台深度对接 |

---

## 4. 策略设计（AI 部分）

作为 AI 工作台，核心策略需显式说明输入信号、模型逻辑、输出形式与兜底规则（参照 PRD 写作规范）。

### 策略 A：每日亮点捕获
- **输入信号**：对话中出现的决策、洞察、学习方法、待办、其他有价值片段。
- **逻辑**：AI 主动识别片段类型 → 生成带 `id` 与类型的结构化行 → 追加到 `highlights.md`。
- **输出**：`highlights.md` 新行；工作台「每日亮点」Tab 立即可见。
- **兜底**：识别不确定时仅做人工手动新增，不臆造内容；每日上限保护避免刷屏。

### 策略 B：记忆沉淀
- **输入信号**：会话结束后的关键结论、用户偏好、项目约定。
- **逻辑**：AI 摘要 → 按主题写入全局 / 项目 MEMORY.md 的 `##` 小节（复用 M3 写回通道）。
- **冷启动**：初始为空，随使用自然增长；不预填噪音。
- **兜底**：写入前与现有小节去重合并，避免重复条目。

---

## 5. 非功能需求

| 维度 | 要求 |
| --- | --- |
| 性能 | 本地单用户；页面首屏 < 1s；写入（含备份）< 200ms；数据量百级条目无感知延迟。 |
| 安全 | 仅绑定 127.0.0.1，无外部网络暴露；写前备份；密钥走环境变量，不落明文文件；提供 `/api/shutdown` 关闭端点。 |
| 兼容性 | Windows 10+ / Python 3.11+ / 现代浏览器（Chromium 内核）；规划 macOS / Linux。 |
| 可靠性 | `.bak` 兜底；端口自动顺延（`find_free_port`）避免冲突；`pythonw` 无黑窗启动。 |
| 可维护性 | 后端纯标准库、前端单文件无构建；数据源（真源文件）与视图（前端）严格分离，便于替换 UI 而不动写回层。 |

---

## 6. 技术架构

### 6.1 总体架构（四层，对齐 2026 AI workspace 范式）

| 层 | 职责 | 当前实现 |
| --- | --- | --- |
| 交互层 Interaction Surface | 一致的操作用户界面（查看 / 编辑 / 删除 / 审批） | `workbench_app.html`（飞书风格表格） |
| 能力适配层 Capability Adapters | 采集 + 写回真实数据源的薄适配层 | `workbench_server.py`（HTTP 写回层） |
| 记忆 / 知识层 Memory Store | 版本化、可检索、可审计的个人上下文 | automations 库 / MEMORY.md / skills / highlights.md |
| 执行内核 Execution Kernel | 调度 agent、执行策略、持久化状态 | 由 WorkBuddy 运行时提供（对话 + automations 调度） |

> 说明：v1 已覆盖交互层 + 能力适配层 + 记忆层；执行内核由 WorkBuddy 运行时承载，工作台作为「可观察 / 可干预的控制面」与之协同。未来 M9 自动沉淀将让执行内核的产出更主动地回流到记忆层，形成**「执行 → 沉淀 → 复用」闭环**。

### 6.2 技术栈

- 后端：Python 3.11 标准库（`http.server` / `sqlite3` / `re` / `json` / `shutil`）。
- 前端：原生 HTML / CSS / JS 单文件，无框架、无构建。
- 启动：`.bat` + `pythonw`（无黑窗）+ 自动打开浏览器。

### 6.3 数据模型

| 实体 | 主键 | 关键字段 | 真源 |
| --- | --- | --- | --- |
| 定时任务 | `id` | 名称 / 状态 / 频率 / 说明 / 下次运行 | automations 数据库 |
| 记忆 | 主题（全局·/项目·标题） | 主题 / 内容 / 来源 | MEMORY.md ## 小节 |
| 技能 | 路径 | 名称 / 位置 / 功能说明 / 路径 | skills/*/SKILL.md + override |
| 每日亮点 | `id`（HL-时间戳） | 类型 / 日期 / 内容 | highlights.md |

### 6.4 接口契约

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/` | 返回飞书风格前端 |
| GET | `/api/data` | 返回 4 类数据 + 计数 + 可筛选维度 |
| POST | `/api/update` | 单字段更新 |
| POST | `/api/update_multi` | 批量多字段更新（一次编辑弹窗改多项） |
| POST | `/api/add` | 新增每日亮点 |
| POST | `/api/delete` | 删除（亮点 / 定时任务软删 / 记忆小节；技能禁用） |
| POST | `/api/shutdown` | 关闭本地服务 |

---

## 7. 数据隐私与安全

- **本地优先**：所有个人数据存于本机文件与本地 SQLite，不上传任何云端。
- **网络边界**：服务仅监听 `127.0.0.1`，外部不可访问。
- **密钥隔离**：第三方凭证走环境变量，不写入工作台管理的任何文件。
- **可恢复性**：写前 `.bak` + 定时任务软删除，误删可在备份中恢复。
- **最小权限**：Skill 删除禁用，避免误删技能文件导致能力丢失。

---

## 8. 发布计划 / 路线图

| 阶段 | 范围 | 状态 |
| --- | --- | --- |
| **MVP**（2026-08） | 本地可编辑工作台：4 分区增 / 改 / 删 / 查 + 搜索 / 筛选 + 自动备份 | 已交付 |
| v1.1 | M8 定时任务新建；亮点按日期排序与筛选增强；空状态引导文案 | 已交付 |
| v1.5 | M9 AI 自动亮点捕获；会话结束记忆沉淀；记忆合并去重 | 规划 |
| v2.0 | M10 跨平台（macOS / Linux）；移动端只读视图；数据导出（Markdown / JSON） | 探索 |
| v3.0（探索） | 技能市场 / 模板化工作流编排；一键打包分发包（安装器 / 容器镜像）；多语言与主题 | 探索 |

---

## 9. 风险与开放问题

### 9.1 风险点

| 风险 | 等级 | 缓解 |
| --- | --- | --- |
| 前端单文件无构建，复杂功能难扩展 | 中 | 视图与写回层解耦；后续可框架化（不影响后端） |
| 写回真源文件可能破坏结构 | 中 | `.bak` + 字段级最小改写；变更前备份 |
| Skill 删除禁用，用户可能困惑 | 低 | UI 文案说明「为避免误删文件不支持删除」 |
| 端口冲突导致起不来 | 低 | `find_free_port` 自动顺延；地址写入 `workbench_addr.txt` |

### 9.2 方向决策（2026-08-13 拍板）

> 用户明确：**只做本地优先（local-first）的开源项目，人人可装、自行托管，暂时不考虑商业化。** 据此关闭前三项开放问题。

- **云端同步（已决策 · 不做）**：纯本地优先，不做任何云端同步/上传。隐私与零依赖优先，移动端只读走导出而非实时同步。
- **产品化路径（已决策 · 开源 self-host）**：定位为 **local-first 开源工具**——`git clone` + 一条命令本地启动，人人可装、自己跑，而非闭源托管的 SaaS。据此「人人可装」= 提供零依赖安装方式（单脚本 / 容器 / requirements），无需账号。
- **商业模式（已决策 · 暂不商业化）**：当前阶段不引入买断/订阅/捐赠等变现，先做好产品与社区。
- **AI 主动度 / 打扰阈值（v1.5 设计参数 · 开放）**：自动捕获亮点与记忆沉淀时，AI 何时"动手写文件 / 何时通知用户"的阈值。详见下方说明，v1.5 落地前需定一个默认值。
  - 维度一 **写入门槛**：一条对话内容要重要/可复用到什么程度，AI 才主动写进 `highlights.md` / `MEMORY.md`（而非只在你手动触发时写）？
  - 维度二 **通知方式**：捕获后是静默写入（不打扰）、轻量 toast 提示、还是弹窗请你确认？
  - 维度三 **越权边界**：默认只"新增/追加"，不自动删除或覆盖既有内容；涉及覆盖/删除必须显式确认。
  - 建议默认值：**静默追加 + 非阻塞 toast 汇总，零弹窗确认；仅"覆盖/删除"类动作才请求确认**。可在设置里调高/调低主动度。

---

## 10. 成功指标

| 维度 | 指标 | 目标 |
| --- | --- | --- |
| 使用率 | 每日打开次数、活跃分区数 | 工作日日均打开 ≥ 1 次 |
| 资产增长 | 亮点 / 记忆条目月净增 | 连续 3 个月正增长 |
| 可靠性 | 写回成功率、误删恢复率 | 均 100% |
| 心流 | 手动在文件间搬运整理的时间 | 较使用前下降 ≥ 50% |

---

## 11. 开源发布（2026-08-13）

- **定位落地**：按 §9.2 决策，项目以 **local-first 开源工具** 形态发布到 **GitHub 与 Gitee 双平台**（均为公开仓库、代码同步镜像），人人可 `git clone` + `python server.py` 本地运行，无需账号、无需联网。
- **与运行时解耦（关键重构）**：发布版 `server.py` 已重写为**完全自包含**（纯 Python 标准库，零依赖）：
  - 不再依赖 WorkBuddy 运行时的 `.workbuddy/` 私有目录与全局 `workbuddy.db`；
  - 数据改存本地 `data/` 目录（可用环境变量 `AI_WORKBENCH_DATA` 覆盖）；
  - 定时任务库改为本地 SQLite `data/automations.db`，首次启动自动建表并写入示例数据；
  - 移除飞书 `sync.py` 依赖与硬编码 token / 绝对路径。
- **隐私保障**：仓库 `.gitignore` 排除 `.workbuddy/`、本地数据库（`data/*.db`）与 `*.bak`；私人数据（记忆、自动化库、飞书配置）绝不入库。
- **交付物**：`server.py`、`workbench_app.html`、`data/`（示例数据）、`README.md`、`LICENSE`（MIT）、`requirements.txt`、`.gitignore`。
- **状态（已发布）**：
  - 本地 git 仓库已 `init`，`main` 分支，初始提交 `be2cb0a` + PRD 章节提交 `f31c0f1`；
  - **GitHub** 公开仓库：`https://github.com/hjunrun02-sys/ai-workbench`（含 v0.1.0 Release）；
  - **Gitee** 公开镜像：`https://gitee.com/msjr123/ai-workbench`（已设为公开，含 v0.1.0 标签）；
  - 本地配置双远程：`origin` = GitHub，`gitee` = Gitee；后续更新由维护者分别推送两个平台。

## 12. 附录

### 术语表
- **MVP**：最小可行产品（Minimum Viable Product）。
- **USP**：独特卖点（Unique Selling Proposition）。
- **HITL**：人在回路（Human-in-the-loop），关键决策保留人工干预。
- **Local-first**：本地优先，数据先落本机、可离线、隐私可控。
- **软删除**：逻辑删除（置标记），不物理移除，便于恢复。

### 参考与依据
- 现有实现：`workbench_server.py`、`workbench_app.html`、`start-workbench.bat`。
- 2026 个人 AI 工作台趋势：local-first、「文件即工作台」、OpenClaw 范式（Soul/Memory/Tool/Identity/User + Skills + Cron，「能力匹配-执行-沉淀-优化」闭环）。
- 标准 PRD 结构（文档信息 / 产品概述 / 用户与场景 / 功能需求 / 非功能需求 / 技术架构 / 数据 / 风险 / 里程碑 / 成功指标 / 附录）。

---

*本文档为草案，版本 v0.1，创建于 2026-08-13，作者 Kai × Miki。关联的已交付代码位于 `.workbuddy/workbench/`。*
